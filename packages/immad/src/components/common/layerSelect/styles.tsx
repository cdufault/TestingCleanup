import styled from 'styled-components';
import { ListItemIcon } from '@mui/material';

const StyledFeatureSelectItemDiv = styled.div`
    display: inline-flex;
    width: 100%;
    align-items: center;
    padding-top: 0;
    padding-bottom: 0;
`;
const StyledListItemIcon = styled(ListItemIcon)`
`;

export { StyledFeatureSelectItemDiv, StyledListItemIcon };
